@using qadi.Models
@model UserViewModel

<tr>
    <form method="post" action="@Url.Action("Add", "User")">
        <td><input asp-for="@Model.user.name" value="" placeholder="Name"/></td>
        <td><input type="submit" name="add" value="Add" /></td>
    </form>
</tr>